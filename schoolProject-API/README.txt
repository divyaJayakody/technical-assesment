
The server project was developed with node Js and Express
The server act as the middleman between the mongodb atlas database and the angular client.
The server supports,
    - retrieving the list of schools from the db
    - inserting a new school to db

To install dependencies
>> npm i

Afterwards, to run the server in a local machine
>> nodemon start
